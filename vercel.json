{
  "buildCommand": "pnpm build --filter=@nexo/web",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/nexo-web/dist",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD .",
  "framework": null,
  "rewrites": [{ "source": "/:path*", "destination": "/index.html" }]
}
